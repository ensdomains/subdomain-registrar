pragma solidity ^0.5.0;

contract Deed {
    address public owner;
    address public previousOwner;

    function value() external view returns (uint256) {
        return 0;
    }

    function closeDeed(uint) public {

    }
}
